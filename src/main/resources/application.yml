server:
  port: 7000
spring:
  application:
    name: recruit-api
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/final_design_graduate?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: root
    password: 1234

# 配置slq打印日志
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-value: 1
      logic-not-delete-value: 0